/**
 * HTTP adapters — REST controllers only.
 * Controllers call service interfaces; they must not depend on repositories or {@code *.impl} types.
 */
package com.example.microservices.order.controller;
