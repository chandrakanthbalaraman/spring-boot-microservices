/**
 * Service implementations — {@code @Service} classes implementing interfaces in {@code service}.
 * Naming: {@code {Feature}ServiceImpl}. Controllers must not depend on this package.
 */
package com.example.microservices.order.service.impl;
