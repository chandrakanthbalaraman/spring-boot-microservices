# Shared style — Phase 3 discovery

Same chrome as api-gateway notebook. Footer **`SB-MS · Phase 03`**. Reference `docs/phases/reference/api-gateway/{n}.jpg`.

Must spell: Eureka, discovery, heartbeat, Kubernetes, inventory-service, localhost.

This phase is **planned** — no runnable `phase-03-service-discovery/` folder yet. Do not invent controller APIs. Ban Gateway as the lesson (trap slide may contrast it). Load balancing = next phase, not the core.
